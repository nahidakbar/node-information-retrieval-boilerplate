"use strict";

const assert = require('assert');
const System = require('.')
  .System;

describe(`System`, function ()
{
  let system;
  beforeEach(async function ()
  {
    system = new System();
  });
});
